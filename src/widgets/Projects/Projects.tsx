import { BaseWidget } from '../../components/BaseWidget/BaseWidget'
import { WidgetProps } from '../types/WidgetProps.types'

export const Projects = ({ widget }: WidgetProps) => {
	return (
		<BaseWidget title='Projects' widget={widget}>
			asd
		</BaseWidget>
	)
}
